(()=>{const videos=[...document.querySelectorAll('.motion-block video')];for(const video of videos){const button=video.parentElement.querySelector('.motion-play');const play=()=>{video.muted=true;video.defaultMuted=true;const result=video.play();if(result)result.then(()=>button.hidden=true).catch(()=>button.hidden=false)};button.textContent='動画を再生';button.addEventListener('click',play);if('IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>{for(const entry of entries){if(entry.isIntersecting)play();else video.pause()}},{rootMargin:'65% 0px',threshold:.01});observer.observe(video)}}const form=document.querySelector('#demo-form');const dialog=document.querySelector('#demo-dialog');form.addEventListener('submit',event=>{event.preventDefault();dialog.showModal()});dialog.querySelector('.close').addEventListener('click',()=>dialog.close());dialog.querySelector('.ok').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',event=>{if(event.target===dialog)dialog.close()})})();